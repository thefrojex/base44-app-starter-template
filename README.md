# App Starter Template

Minimal Vite, React, TypeScript, Tailwind CSS, shadcn/ui, Supabase, and TanStack Query starter for generated apps.

## Setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` and set the Supabase values when the app needs Supabase access.
